export default function DashboardPage() {
  return (
    <section className="mx-auto mt-10 max-w-2xl rounded-lg border border-line bg-surface p-6 shadow-sm">
      <h1 className="font-serif text-2xl font-semibold text-primary">Seller Dashboard</h1>
      <p className="mt-3 text-sm leading-6 text-muted">
        Seller-only API routes are guarded in Phase 1. The dashboard interface arrives in Phase 5.
      </p>
    </section>
  );
}
